import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary ──
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryLight = Color(0xFFBBDEFB);
  static const Color primaryDark = Color(0xFF1565C0);

  // ── Accent ──
  static const Color accent = Color(0xFF448AFF);

  // ── Background ──
  static const Color scaffoldBg = Color(0xFFFFFFFF);
  //splash
  static const Color splashBg = Color.fromARGB(255, 11, 91, 15);

  static const Color cardBg = Color(0xFFF5F5F5);
  static const Color surfaceBg = Color(0xFFFAFAFA);

  // ── Text ──
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // ── Splash Specific ──
  static const Color splashLoader = Color(0xFF448AFF);

  // ── General ──
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color transparent = Colors.transparent;
  static const Color divider = Color(0xFFE0E0E0);
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
}
